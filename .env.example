# Application Settings
PORT=3000
NODE_ENV=development

# Database Connection URL (Prisma)
# PostgreSQL connection string for the Ape Archive database
DATABASE_URL="postgresql://root:toor@45.159.221.130:5515/apearchive"

# JWT Configuration
JWT_SECRET=your-secret-key-here
JWT_REFRESH_SECRET=your-refresh-secret-here

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Google Drive Integration (Required for Drive Sync)
# 1. Create OAuth2 credentials at https://console.cloud.google.com/
# 2. Generate a refresh token using Google OAuth2 Playground
# 3. Create a folder in Google Drive for content storage (ROOT_FOLDER_ID - the source to migrate from)
# 4. Create a separate flat folder for uploads (UPLOAD_FOLDER_ID - where all files will be moved)
# 5. Set folder IDs from the URLs: 
#    https://drive.google.com/drive/folders/ROOT_FOLDER_ID_HERE
#    https://drive.google.com/drive/folders/UPLOAD_FOLDER_ID_HERE

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REFRESH_TOKEN=your-refresh-token
ROOT_FOLDER_ID=your-source-drive-folder-id
UPLOAD_FOLDER_ID=your-flat-upload-folder-id

# Optional: Enable debug logging
DEBUG=ape-archive:*
